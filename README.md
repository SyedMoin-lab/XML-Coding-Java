# XML-Coding-Java
This Java console application validates XML strings by ensuring that each opening tag has a corresponding closing tag and that the tags are properly nested. It provides a straightforward method to check XML well-formedness using a custom implementation without relying on external libraries.
